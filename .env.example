# Base URL for the employee REST API. Defaults to JSONPlaceholder (a public,
# keyless mock API) if not set. Copy this file to .env to override it.
VITE_API_BASE_URL=https://jsonplaceholder.typicode.com
